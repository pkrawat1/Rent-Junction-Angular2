.row.top-bar
  header.row
    section.float-right
      small
        a(href='#') 24/7 Customer Support 
        | | 
        a(href='#') Select City 
        | | 
        a(
          ng-if="!_auth.isAuthenticated()"
          ng-click='_auth.toggleSignUpModal()'
          ) Sign Up 
        | | 
        a(
          ng-if="!_auth.isAuthenticated()"
          ng-click= "_auth.toggleSignInModal()"
          ) Log In 
        a(
          href='#'
          ng-if="_auth.isAuthenticated()"
          ng-click= '_auth.logout()'
          ) Log Out 
  br
  nav.row(data-topbar='', role='navigation')
    // Top Bar Section
    section.top-bar-left
      // Top Bar Left Nav Elements
      ul.menu
        li.large-2.small-2.columns.float-left
          a.menu-text(href='#/home') Rent Junction
        // Search | has-form wrapper
        li.large-8.small-8.columns.float-center
          .input-group
            span.input-group-label.button.hollow.fi-magnifying-glass.large
            input.input-group-field(
              type='text'
              placeholder='So, What are you waiting for?'
            )
            a.input-group-button.button(href='#', ng-model= 'q')
              strong Search
        li.large-2.small-2.columns.float-right
          a.button.warning.hollow
            strong
              i.fi-shopping-cart
              |  Cart  
              .badge.warning.large 0
